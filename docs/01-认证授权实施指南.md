# 认证授权实施指南

## 📋 概述

本文档提供Spring Security + JWT认证授权机制的完整实施方案，是整个安全加固的**核心基础**。

**优先级**：P0（必须立即实施）  
**预计工作量**：3-5天  
**前置依赖**：无  
**影响范围**：后端所有Controller、前端所有API调用

## 🎯 实施目标

- ✅ 所有API端点需要JWT Token认证
- ✅ 用户登录后获取Token，存储在HttpOnly Cookie
- ✅ 实现Token刷新机制，避免频繁登录
- ✅ 用户数据完全隔离，无法访问其他用户数据
- ✅ 支持登出功能（Token失效）
- ✅ 前端自动处理401未授权，跳转登录页

## 🏗️ 架构设计

### 认证流程

```
┌─────────┐         ┌──────────┐         ┌─────────┐
│  前端   │         │  后端    │         │ 数据库  │
└────┬────┘         └────┬─────┘         └────┬────┘
     │                   │                    │
     │ 1. POST /login    │                    │
     │ {username, pwd}   │                    │
     ├──────────────────>│                    │
     │                   │ 2. 查询用户        │
     │                   ├───────────────────>│
     │                   │<───────────────────┤
     │                   │ 3. BCrypt验证密码  │
     │                   │                    │
     │                   │ 4. 生成JWT Token   │
     │ 5. 返回Token      │                    │
     │ (HttpOnly Cookie) │                    │
     │<──────────────────┤                    │
     │                   │                    │
     │ 6. GET /api/xxx   │                    │
     │ (Cookie自动携带)  │                    │
     ├──────────────────>│                    │
     │                   │ 7. 解析验证Token   │
     │                   │ 8. 提取userId      │
     │                   │ 9. 查询数据        │
     │                   │ (WHERE user_id=?)  │
     │                   ├───────────────────>│
     │                   │<───────────────────┤
     │ 10. 返回数据      │                    │
     │<──────────────────┤                    │
     │                   │                    │
```

### Token结构

JWT Token由三部分组成（Base64编码，用`.`分隔）：

```
Header.Payload.Signature
```

**Payload示例**：
```json
{
  "sub": "testuser",        // 用户名
  "userId": 1,              // 用户ID
  "iat": 1629820800,        // 签发时间
  "exp": 1629907200         // 过期时间（24小时后）
}
```

### 数据库改造

**无需改动表结构**，但需要确保`userlogin`表有主键：

```sql
-- 检查表结构
DESC userlogin;

-- 如果没有id主键，添加：
ALTER TABLE userlogin ADD COLUMN id INT AUTO_INCREMENT PRIMARY KEY FIRST;
```

## 🔧 实施步骤

### 第一步：添加依赖

编辑 `backend/pom.xml`，添加Spring Security和JWT依赖：

```xml
<dependencies>
    <!-- 现有依赖... -->
    
    <!-- Spring Security -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    
    <!-- JWT支持 -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.3</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>0.12.3</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>0.12.3</version>
        <scope>runtime</scope>
    </dependency>
</dependencies>
```

### 第二步：配置JWT密钥

编辑 `backend/src/main/resources/application.yml`：

```yaml
jwt:
  secret: ${JWT_SECRET:changeme-please-use-a-very-long-random-string-at-least-256-bits}
  expiration: 86400000  # 24小时（毫秒）
  refresh-expiration: 604800000  # 7天（毫秒）
  cookie-name: linshu_token
```

**生产环境必须设置环境变量**：
```bash
export JWT_SECRET="your-super-secret-key-change-this-in-production"
```

**生成强密钥**：
```bash
openssl rand -base64 64
```

### 第三步：创建JWT工具类

创建 `backend/src/main/java/com/example/backend/security/JwtUtil.java`：

```java
package com.example.backend.security;

import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;

@Component
public class JwtUtil {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private Long expiration;
    
    private SecretKey getSigningKey() {
        byte[] keyBytes = secret.getBytes(StandardCharsets.UTF_8);
        return Keys.hmacShaKeyFor(keyBytes);
    }
    
    /**
     * 生成Token
     * @param username 用户名
     * @param userId 用户ID
     */
    public String generateToken(String username, Integer userId) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);
        
        return Jwts.builder()
                .subject(username)
                .claim("userId", userId)
                .issuedAt(now)
                .expiration(expiryDate)
                .signWith(getSigningKey())
                .compact();
    }
    
    /**
     * 从Token中提取用户名
     */
    public String getUsernameFromToken(String token) {
        Claims claims = parseToken(token);
        return claims.getSubject();
    }
    
    /**
     * 从Token中提取用户ID
     */
    public Integer getUserIdFromToken(String token) {
        Claims claims = parseToken(token);
        return claims.get("userId", Integer.class);
    }
    
    /**
     * 验证Token是否有效
     */
    public boolean validateToken(String token) {
        try {
            parseToken(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }
    
    /**
     * 解析Token
     */
    private Claims parseToken(String token) {
        return Jwts.parser()
                .verifyWith(getSigningKey())
                .build()
                .parseSignedClaims(token)
                .getPayload();
    }
    
    /**
     * 检查Token是否即将过期（1小时内）
     */
    public boolean shouldRefresh(String token) {
        try {
            Claims claims = parseToken(token);
            Date expiration = claims.getExpiration();
            Date now = new Date();
            long timeLeft = expiration.getTime() - now.getTime();
            return timeLeft < 3600000; // 小于1小时
        } catch (Exception e) {
            return false;
        }
    }
}
```

### 第四步：创建认证过滤器

创建 `backend/src/main/java/com/example/backend/security/JwtAuthenticationFilter.java`：

```java
package com.example.backend.security;

import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.Cookie;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.Collections;

@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Value("${jwt.cookie-name}")
    private String cookieName;
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                    HttpServletResponse response, 
                                    FilterChain filterChain) 
            throws ServletException, IOException {
        
        try {
            String jwt = extractJwtFromCookie(request);
            
            if (jwt != null && jwtUtil.validateToken(jwt)) {
                String username = jwtUtil.getUsernameFromToken(jwt);
                Integer userId = jwtUtil.getUserIdFromToken(jwt);
                
                // 创建认证对象
                UsernamePasswordAuthenticationToken authentication = 
                    new UsernamePasswordAuthenticationToken(
                        new UserPrincipal(userId, username), 
                        null, 
                        Collections.emptyList()
                    );
                authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                
                // 设置到Security Context
                SecurityContextHolder.getContext().setAuthentication(authentication);
            }
        } catch (Exception e) {
            logger.error("无法设置用户认证", e);
        }
        
        filterChain.doFilter(request, response);
    }
    
    /**
     * 从Cookie中提取JWT Token
     */
    private String extractJwtFromCookie(HttpServletRequest request) {
        Cookie[] cookies = request.getCookies();
        if (cookies != null) {
            for (Cookie cookie : cookies) {
                if (cookieName.equals(cookie.getName())) {
                    return cookie.getValue();
                }
            }
        }
        return null;
    }
}
```

### 第五步：创建用户主体类

创建 `backend/src/main/java/com/example/backend/security/UserPrincipal.java`：

```java
package com.example.backend.security;

import lombok.AllArgsConstructor;
import lombok.Data;

@Data
@AllArgsConstructor
public class UserPrincipal {
    private Integer userId;
    private String username;
}
```

### 第六步：配置Spring Security

创建 `backend/src/main/java/com/example/backend/security/SecurityConfig.java`：

```java
package com.example.backend.security;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Autowired
    private JwtAuthenticationFilter jwtAuthenticationFilter;
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // 禁用CSRF（使用JWT不需要）
            .csrf(csrf -> csrf.disable())
            
            // 无状态Session
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            
            // 配置URL权限
            .authorizeHttpRequests(auth -> auth
                // 允许注册和登录接口
                .requestMatchers("/user/register", "/user/login").permitAll()
                // WebSocket需要特殊处理（后续配置）
                .requestMatchers("/ws/**").permitAll()
                // 其他所有请求都需要认证
                .anyRequest().authenticated()
            )
            
            // 添加JWT过滤器
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }
}
```

### 第七步：改造UserController

编辑 `backend/src/main/java/com/example/backend/controller/UserController.java`：

```java
package com.example.backend.controller;

import com.example.backend.entity.UserLogin;
import com.example.backend.security.JwtUtil;
import com.example.backend.service.UserService;
import jakarta.servlet.http.Cookie;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.http.ResponseEntity;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.web.bind.annotation.*;

import java.util.HashMap;
import java.util.Map;

@RestController
@RequestMapping("/user")
public class UserController {
    
    @Autowired
    private UserService userService;
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Value("${jwt.cookie-name}")
    private String cookieName;
    
    /**
     * 用户注册
     */
    @PostMapping("/register")
    public ResponseEntity<?> register(@RequestBody UserLogin user) {
        try {
            // 检查用户名是否已存在
            UserLogin existingUser = userService.selectUserByUsername(user.getUsername());
            if (existingUser != null) {
                return ResponseEntity.badRequest().body(Map.of(
                    "success", false,
                    "message", "用户名已存在"
                ));
            }
            
            // 密码加密
            String encodedPassword = passwordEncoder.encode(user.getPassword());
            user.setPassword(encodedPassword);
            
            // 保存用户
            boolean success = userService.insertUser(user);
            if (success) {
                return ResponseEntity.ok(Map.of(
                    "success", true,
                    "message", "注册成功"
                ));
            } else {
                return ResponseEntity.status(500).body(Map.of(
                    "success", false,
                    "message", "注册失败"
                ));
            }
        } catch (Exception e) {
            return ResponseEntity.status(500).body(Map.of(
                "success", false,
                "message", "系统错误：" + e.getMessage()
            ));
        }
    }
    
    /**
     * 用户登录
     */
    @PostMapping("/login")
    public ResponseEntity<?> login(@RequestBody UserLogin loginRequest, 
                                    HttpServletResponse response) {
        try {
            // 查询用户
            UserLogin user = userService.selectUserByUsername(loginRequest.getUsername());
            if (user == null) {
                return ResponseEntity.badRequest().body(Map.of(
                    "success", false,
                    "message", "用户名或密码错误"
                ));
            }
            
            // 验证密码
            if (!passwordEncoder.matches(loginRequest.getPassword(), user.getPassword())) {
                return ResponseEntity.badRequest().body(Map.of(
                    "success", false,
                    "message", "用户名或密码错误"
                ));
            }
            
            // 生成Token
            String token = jwtUtil.generateToken(user.getUsername(), user.getId());
            
            // 设置HttpOnly Cookie
            Cookie cookie = new Cookie(cookieName, token);
            cookie.setHttpOnly(true);
            cookie.setPath("/");
            cookie.setMaxAge(24 * 60 * 60); // 24小时
            // 生产环境必须启用：
            // cookie.setSecure(true); // 仅HTTPS
            response.addCookie(cookie);
            
            return ResponseEntity.ok(Map.of(
                "success", true,
                "message", "登录成功",
                "username", user.getUsername()
            ));
        } catch (Exception e) {
            return ResponseEntity.status(500).body(Map.of(
                "success", false,
                "message", "系统错误：" + e.getMessage()
            ));
        }
    }
    
    /**
     * 用户登出
     */
    @PostMapping("/logout")
    public ResponseEntity<?> logout(HttpServletResponse response) {
        // 清除Cookie
        Cookie cookie = new Cookie(cookieName, null);
        cookie.setHttpOnly(true);
        cookie.setPath("/");
        cookie.setMaxAge(0);
        response.addCookie(cookie);
        
        return ResponseEntity.ok(Map.of(
            "success", true,
            "message", "登出成功"
        ));
    }
    
    /**
     * 获取当前用户信息
     */
    @GetMapping("/current")
    public ResponseEntity<?> getCurrentUser(@RequestAttribute("userId") Integer userId) {
        try {
            UserLogin user = userService.selectUserById(userId);
            if (user != null) {
                // 不返回密码
                user.setPassword(null);
                return ResponseEntity.ok(Map.of(
                    "success", true,
                    "user", user
                ));
            } else {
                return ResponseEntity.status(404).body(Map.of(
                    "success", false,
                    "message", "用户不存在"
                ));
            }
        } catch (Exception e) {
            return ResponseEntity.status(500).body(Map.of(
                "success", false,
                "message", "系统错误"
            ));
        }
    }
}
```

### 第八步：创建获取当前用户的工具类

创建 `backend/src/main/java/com/example/backend/security/SecurityUtils.java`：

```java
package com.example.backend.security;

import org.springframework.security.core.Authentication;
import org.springframework.security.core.context.SecurityContextHolder;

public class SecurityUtils {
    
    /**
     * 获取当前登录用户ID
     */
    public static Integer getCurrentUserId() {
        Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        if (authentication != null && authentication.getPrincipal() instanceof UserPrincipal) {
            return ((UserPrincipal) authentication.getPrincipal()).getUserId();
        }
        return null;
    }
    
    /**
     * 获取当前登录用户名
     */
    public static String getCurrentUsername() {
        Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        if (authentication != null && authentication.getPrincipal() instanceof UserPrincipal) {
            return ((UserPrincipal) authentication.getPrincipal()).getUsername();
        }
        return null;
    }
}
```

### 第九步：改造所有Controller实现用户隔离

**示例：MonitorController**

```java
@GetMapping("/dashboard")
public ResponseEntity<?> getDashboard() {
    // 获取当前用户ID
    Integer userId = SecurityUtils.getCurrentUserId();
    
    // 查询时自动过滤用户数据
    List<ComponentConfig> servers = componentConfigService.selectByUserId(userId);
    
    // ... 其他逻辑
}
```

**关键点**：
- 所有查询都必须带上 `WHERE user_id = ?`
- 删除/更新操作前必须验证数据所有权
- 不要相信前端传来的userId

### 第十步：前端改造

#### 10.1 修改登录页面

编辑 `front/src/views/Login.vue`：

```javascript
const handleLogin = async () => {
  try {
    const response = await axios.post('/user/login', {
      username: username.value,
      password: password.value
    }, {
      withCredentials: true  // 重要：允许发送Cookie
    })
    
    if (response.data.success) {
      ElMessage.success('登录成功')
      // 不再存储到localStorage
      // localStorage.setItem('user', username.value)
      router.push('/')
    } else {
      ElMessage.error(response.data.message)
    }
  } catch (error) {
    ElMessage.error('登录失败')
  }
}
```

#### 10.2 配置Axios拦截器

编辑 `front/src/main.js` 或创建 `front/src/utils/request.js`：

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from './router'

// 配置Axios
axios.defaults.withCredentials = true  // 全局允许发送Cookie

// 响应拦截器
axios.interceptors.response.use(
  response => response,
  error => {
    if (error.response && error.response.status === 401) {
      ElMessage.error('登录已过期，请重新登录')
      router.push('/login')
    }
    return Promise.reject(error)
  }
)
```

#### 10.3 配置路由守卫

编辑 `front/src/router/index.js`：

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import axios from 'axios'

const router = createRouter({
  // ... 路由配置
})

// 路由守卫
router.beforeEach(async (to, from, next) => {
  if (to.path === '/login' || to.path === '/register') {
    next()
    return
  }
  
  try {
    // 检查是否已登录（调用获取当前用户接口）
    await axios.get('/user/current')
    next()
  } catch (error) {
    next('/login')
  }
})

export default router
```

### 第十一步：UserService改造

确保 `UserService` 有以下方法：

```java
public interface UserService {
    UserLogin selectUserByUsername(String username);
    UserLogin selectUserById(Integer id);
    boolean insertUser(UserLogin user);
}
```

对应的Mapper：

```java
@Mapper
public interface UserMapper extends BaseMapper<UserLogin> {
    @Select("SELECT * FROM userlogin WHERE username = #{username}")
    UserLogin selectByUsername(String username);
}
```

### 第十二步：数据迁移脚本

现有明文密码需要重新加密，提供两种方案：

**方案A：要求用户重置密码（推荐）**

```sql
-- 1. 备份原表
CREATE TABLE userlogin_backup AS SELECT * FROM userlogin;

-- 2. 清空密码字段，强制用户重置
UPDATE userlogin SET password = '';

-- 3. 添加标记列
ALTER TABLE userlogin ADD COLUMN password_reset_required TINYINT DEFAULT 1;
```

然后发送邮件通知用户重置密码。

**方案B：生成临时密码**

```java
// 一次性迁移脚本（不要提交到代码库）
@Component
public class PasswordMigration implements CommandLineRunner {
    
    @Autowired
    private UserMapper userMapper;
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    @Override
    public void run(String... args) throws Exception {
        List<UserLogin> users = userMapper.selectList(null);
        for (UserLogin user : users) {
            // 生成临时密码
            String tempPassword = UUID.randomUUID().toString().substring(0, 8);
            String encoded = passwordEncoder.encode(tempPassword);
            user.setPassword(encoded);
            userMapper.updateById(user);
            
            // 记录临时密码（需要发送给用户）
            System.out.println("User: " + user.getUsername() + " TempPwd: " + tempPassword);
        }
    }
}
```

## ✅ 测试验证

### 1. 功能测试

```bash
# 1. 注册新用户
curl -X POST http://localhost:8080/user/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"Test@123"}'

# 2. 登录获取Token
curl -X POST http://localhost:8080/user/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"Test@123"}' \
  -c cookies.txt

# 3. 访问受保护接口（携带Cookie）
curl -X GET http://localhost:8080/api/system/dashboard \
  -b cookies.txt

# 4. 测试未授权访问（不带Cookie）
curl -X GET http://localhost:8080/api/system/dashboard
# 应该返回401
```

### 2. 安全测试

```bash
# 测试Token伪造
curl -X GET http://localhost:8080/api/system/dashboard \
  -H "Cookie: linshu_token=fake.token.here"
# 应该返回401

# 测试过期Token
# （等待24小时后重试，应该自动跳转登录）

# 测试用户隔离
# 1. 用户A登录，创建服务器配置
# 2. 用户B登录，尝试访问A的数据
# 应该看不到A的数据
```

### 3. 性能测试

```bash
# 测试认证性能开销
ab -n 1000 -c 10 -C "linshu_token=your-valid-token" \
  http://localhost:8080/api/system/dashboard
```

预期：JWT验证开销 < 10ms/请求

## 🚨 常见问题

### Q1: 为什么使用HttpOnly Cookie而不是localStorage？

**A**: HttpOnly Cookie无法被JavaScript读取，防止XSS攻击窃取Token。localStorage可以被任意JavaScript访问。

### Q2: 如何处理WebSocket认证？

WebSocket无法设置自定义HTTP Header，但可以读取Cookie：

```java
// ServerWebSocketHandler.java
@Override
public void afterConnectionEstablished(WebSocketSession session) throws Exception {
    // 从Cookie中提取Token
    String token = extractTokenFromSession(session);
    if (!jwtUtil.validateToken(token)) {
        session.close();
        return;
    }
    Integer userId = jwtUtil.getUserIdFromToken(token);
    // ... 绑定userId到session
}
```

### Q3: Token刷新机制如何实现？

```java
@PostMapping("/refresh")
public ResponseEntity<?> refreshToken(@CookieValue("linshu_token") String oldToken,
                                      HttpServletResponse response) {
    if (jwtUtil.shouldRefresh(oldToken)) {
        String username = jwtUtil.getUsernameFromToken(oldToken);
        Integer userId = jwtUtil.getUserIdFromToken(oldToken);
        String newToken = jwtUtil.generateToken(username, userId);
        
        // 设置新Cookie
        Cookie cookie = new Cookie(cookieName, newToken);
        cookie.setHttpOnly(true);
        cookie.setPath("/");
        cookie.setMaxAge(24 * 60 * 60);
        response.addCookie(cookie);
        
        return ResponseEntity.ok(Map.of("success", true));
    }
    return ResponseEntity.ok(Map.of("success", false, "message", "Token still valid"));
}
```

### Q4: 如何支持"记住我"功能？

```java
// 登录时添加rememberMe参数
if (rememberMe) {
    cookie.setMaxAge(7 * 24 * 60 * 60); // 7天
} else {
    cookie.setMaxAge(24 * 60 * 60); // 24小时
}
```

### Q5: 如何实现多设备登录管理？

当前方案支持多设备同时登录（每个设备独立Token）。如需限制：

1. Token中添加设备指纹
2. 维护用户-Token映射表（Redis）
3. 新登录时使旧Token失效

### Q6: 生产环境部署注意事项

```yaml
# application-prod.yml
jwt:
  secret: ${JWT_SECRET}  # 必须从环境变量读取
  cookie-name: __Host-linshu_token  # 启用Cookie前缀

server:
  servlet:
    session:
      cookie:
        secure: true  # 强制HTTPS
        same-site: strict  # 防CSRF
```

## 📊 性能影响评估

| 操作 | 额外开销 | 说明 |
|------|---------|------|
| 登录 | +100-200ms | BCrypt密码验证 |
| API请求 | +5-10ms | JWT解析验证 |
| 内存 | +50MB | Spring Security框架 |
| 数据库 | 无 | 无需Session表 |

## 🎉 完成检查清单

- [ ] 添加Spring Security和JWT依赖
- [ ] 配置JWT密钥（环境变量）
- [ ] 创建JwtUtil工具类
- [ ] 创建JwtAuthenticationFilter过滤器
- [ ] 配置SecurityConfig
- [ ] 改造UserController（注册/登录/登出）
- [ ] 改造所有Controller实现用户隔离
- [ ] 改造UserService和Mapper
- [ ] 前端改造：登录页面、Axios配置、路由守卫
- [ ] 数据迁移：用户密码重新加密
- [ ] 功能测试：注册、登录、访问受保护资源
- [ ] 安全测试：Token伪造、过期、用户隔离
- [ ] 性能测试：认证开销验证
- [ ] 文档更新：API文档、部署文档

## 📝 下一步

完成本文档后，继续实施：
- [02-密码加密实施指南.md](./02-密码加密实施指南.md) - SSH密码加密存储
- [03-配置安全实施指南.md](./03-配置安全实施指南.md) - 敏感配置外部化

---

**重要提醒**：本方案必须在测试环境充分验证后再部署生产！
